#include <math.h>
#include <stdio.h>
#include <limits.h>

#include "tests.h"

void generateAllCombinations(int** numbers, char** operators) {
}

int main()
{
#if ENABLE_TESTS > 0
    run_tests(true);
#endif
    return 0;

}
